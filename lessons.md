# BrainDance Lessons

这里记录可复用的短原则：写成“以后应当如何做”，不记录某次提交、命令输出或排查过程。

## 并发与资源

- 资源一经取得，先建立 cleanup guard，再做任何可能抛错的初始化。
- 锁、连接和数据库写入必须属于同一 authority；不能用“同一环境变量”替代同一 authority。
- 持锁代码若还需要数据库访问，必须明确每个请求占用的 session 数；池容量饱和时应排队前进，不能等待第二条连接。
- 外部 I/O 不能放入数据库事务；但锁是否跨越外部 I/O 必须由幂等语义和资源上限共同决定。

## 多人可见内容

- 多人内容的可见性必须由服务端按读取者、来源和时间窗口裁决；前端隐藏不是访问控制。
- 延迟公开时，答案、评论、引用和回复都要按同一信息泄露边界审查；家长的普通提醒可以即时可见，能暴露私密答案的线程不能例外。
- 把“学生私有目录”改成“家庭共享项目”时，必须同时修改列表读取、申请写入、限次锁键和家长聚合展示；只改页面名称或布局会制造看似完成、实际仍为空的假象。
- 分接收人存储、共享讨论展示时，读取范围、回复/引用写入校验和引用摘要投影必须使用同一个“已公开兄弟投递”集合；不能让各页面依赖当前卡片内的临时数组恢复引用。
- 引用关系的稳定契约是“事实 ID + 服务端授权后的作者/摘要投影”；界面不得展示裸 ID，也不得因目标不在当前分栏就丢掉引用。

## 迁移与事实

- 迁移兼容性必须在任何 schema/ledger mutation 前检查；事后报错不构成 gate。
- 历史事实不能以“无法回填”为由静默删除；必须可验证回填或 fail closed。
- 旧环境的 migration ledger 与当前 lineage 不兼容时，先用新建隔离库验证；不得为通过门禁而改写或清空既有账本。
- 新增 API 所依赖的表在隔离库测试通过后，仍须在实际运行库核对迁移账本和目标表；不能把“代码已合并”当作“试点已部署”。
- 历史 migration checksum 不一致时，迁移门禁必须 fail closed；不得为了上线后续迁移而跳过 gate、改写账本或手工伪造已应用状态，先保留原库并取得重建授权。
- 将“单条作答”改为“多次作答”时，每次提交必须是新事实；幂等键只重放同一提交，不能把不同提交伪装成版本覆盖。
- 优先级覆盖不能删除低优先级日程；保留原日程并记录覆盖来源，开始、完成和过期结算都必须排除被覆盖项。
- 会执行 reset/TRUNCATE 的测试必须在命令中显式指定隔离测试库；不得让测试隐式继承 `.env.local` 的默认开发库。
- 测试帮助器自身也必须拒绝非 `*_test_*`、`*_isolated_*` 或 `*_e2e_*` 数据库；命令遗漏环境变量时应在连接前失败，而不是靠约定避免误清库。
- 共享按钮默认不是 submit 时，所有表单的保存按钮都必须显式写 `type="submit"`，并用浏览器测试断言真实 POST/PATCH 请求；视觉可点击不等于表单会提交。
- 运行中的 Next 开发服务不能与同目录生产构建共用 `.next`；构建会替换静态清单，旧服务可能继续返回指向已删除 chunk 的 HTML。浏览器回归必须隔离产物目录或在构建后重启服务。

## 验证与协作

- UI 审查必须区分真实业务页面与浏览器示例数据；除页面整体宽度外，还应检查导航、日历和弹窗内部的滚动、内容可达性与键盘焦点。
- 开发预览的截图和 JSON 证据先写到源码监视范围之外，采集结束后统一复制，避免热重载污染截图。
- 规格中的规则、schema 预留字段和已运行功能必须区分；宣称设置可用前检查页面入口、写入契约和实际执行链路，不能把“有字段”当成“已生效”。
- 文件上传可用性必须从真实启动入口验证存储目录和扫描器整条链路；内存存储或 always-clean 测试替身通过不代表本机上传可用。
- 并发、事务和状态机验收以不变量与故障矩阵为准，不能只看主路径测试全绿。
- 测试不得吞 fixture 失败、使用恒真断言或手工写入目标终态替代业务路径。
- 设计级缺陷先冻结完整替代设计，再一次实现；不要用连续小 Prompt 修补同一模型。
- 内部风险检查不等于对外阶段：默认以一次完整实现和一次集中整改交付用户需求。
- 本机服务按端口可访问不代表它连接了预期数据库；切换隔离库前必须识别并替换旧服务进程，再做真实登录回归。
- 纯表现层的个人偏好应先使用浏览器本地存储，并以服务端可渲染的默认值避免首屏闪烁；只有明确需要跨设备同步时才进入账户数据模型。
- 离开仍在写入或计时的工作流时，确认弹窗不能替代终止操作；必须先让权威状态机成功进入终态，再执行导航。
- 视觉改造必须改变信息层级并检查真实页面截图，不能把换色、圆角和菜单卡片当成首页重设计。
- “我的”必须对应本人资料，显示姓名/账号；内部 ID 不是面向用户的身份展示。
- 新建子账号和关联已有账号是不同授权场景：前者可按明确产品规则原子建立关系，后者不能省略既有账号确认。
- 锁定问题先核对实际数据库、密码校验与安全事件；过期解锁必须重置失败计数，不能关闭所有环境的安全门禁掩盖体验问题。
- 改动创建流程时同步修改 bootstrap 与端到端夹具，避免夹具再执行已自动完成的步骤；密码输入框用稳定标识定位，不能假定 password input 有 textbox role。
- 本机验收要区分应用失败与数据库/Docker 不可用；基础设施启动失败时保留数据、记录未完成验证，不能靠重置环境或重复修改业务代码掩盖问题。
- 面向 Windows PowerShell 5.1 的 `.ps1` 启动路径不要依赖无 BOM UTF-8 的非 ASCII 异常文本；关键启动/错误信息使用 ASCII，且用 `powershell.exe` 的 parser 做回归检查。
- 手动生成跨计划切换日的日程时，不能把整段范围拒绝；必须只处理与该计划生效区间的交集，并向用户返回实际生成日期。
- 计划库接管学生时，不能只关闭计划库自己的 activation；必须同时处理历史正式计划，否则数据库的“每学生仅一个启用正式计划”约束会把请求变成 500。
- 弹窗组件必须区分普通层和错误层；错误层必须高于当前业务弹窗，不能只给所有弹窗相同 z-index。
- 可编辑列表项的 React key 必须与用户可改字段无关；用输入值作 key 会在每个字符后重挂载并丢失焦点。
- 迁移一级导航到实体二级页签时，必须逐个替换原入口、保留同等列表能力并为旧 URL 提供重定向；只新增页签或复用页面组件不算完成迁移。
- 本机存在开发库和闭测运行库时，重建、迁移和验收必须从实际服务启动命令反查目标库；不能依据 `.env.local` 或端口可访问性推断。跨库的破坏性授权必须重新确认精确数据库名。
- 增量迁移也必须以启动脚本最终改写后的数据库 URL 为 authority；基础 URL 迁移成功不能替代实际运行库核对。
- Windows 启动脚本可直接调用仓库内已安装包的 Node 入口；不要让 `pnpm exec` 的 PATH/shim 差异成为服务启动单点故障。
- 同一推送的“内容库详情”与“某学生投递详情”是不同视图：家长全局列表必须在当前详情容器内逐投递展示作答和评论，不能只给二次跳转链接。
- 同一内容条目的多次投递可以处于不同生命周期状态；聚合详情只能按每条投递的实时状态决定是否呈现写操作，不能用内容级列表状态推断。
- 业务“进行中”若由事实派生，查询 DTO、有效状态和视觉映射必须同时接入；只写事实不等于用户能看到状态变化。
- 清理日程应软取消且明确排除已开始/已完成事实；学生的“自己生成”必须落到服务端可验证的 owner authority。
- 首次完成时声明执行区间与完成后的正式事实更正是两个概念，不能共用确认、配额和结算口径。
- 兑换等扣减型操作必须“客户端提前提示 + 服务端权威门禁 + 提交前确认”三层同时存在。
- 评论树以 parent ID 建层级，以授权后的引用投影展示语义；CSS 缩进不能替代事实引用关系。
- 修复依赖目录必须是原子流程：完整删除成功后才能按锁文件安装；出现拒绝访问或残留时立即停止，禁止覆盖半删除目录。
- 依赖故障最终必须用用户原命令和真实页面/静态资源验证；包目录存在、类型检查通过或从根目录加载传递依赖都不能替代启动验收。

- 聚合详情中的共享附件只在内容层展示一次；逐对象互动卡只承载该对象的状态和讨论，避免把共享内容误认为某个对象的作答。
- “支持个人计划”必须验证冻结积分规则确实归零；只看到能创建、能生成，不能证明不会进入积分结算。
- 编辑已发布内容应追加版本并同步其实际投递实体；只改内容库标题会造成“界面说已编辑、接收方仍看旧内容”的假成功。
- 隐藏生成对象选择器不等于授权：必须把当前上下文明示给用户，并保留服务端的绑定、owner 与实时关系校验。
- 家长代办学生任务不能只改前端角色门禁；必须在服务端复核关系、保留开始命令审计，并让代为完成的事件/事实与不可变积分流水可追溯到操作者。

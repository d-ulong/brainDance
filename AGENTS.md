# BrainDance Engineering Guide

本文件适用于仓库内全部工作。明确的用户要求和更具体的项目规格优先。

## 1. 项目背景

BrainDance 是面向 5–18 岁学生及其家庭的学习计划、认知训练和积分协作平台。首期采用中国大陆、邀请制、固定 `Asia/Shanghai` 时区的家庭场景。

核心产品边界：

- 学生完成训练、计划和日程，系统保留可解释、可追溯的事实与趋势。
- 家长只能在生效家庭关系和资源授权范围内陪伴、配置和查看。
- 积分通过版本化规则、不可变流水和可重建余额投影结算。
- 不提供医学或智力诊断、排行榜、AI/语音、电商支付或家庭外组织能力。
- 术语以 `CONTEXT.md` 为准，产品范围与验收以 `docs/` 和当前 Trellis 任务为准。

## 2. 技术架构

- TypeScript 全栈模块化单体：Next.js 15 App Router、React 19、PostgreSQL、Drizzle ORM、独立 Worker。
- API Route 保持薄层；业务逻辑位于 `src/modules/`，数据库 schema/migration 位于 `src/db/`。
- PostgreSQL 保存权威事实；余额、成员、首页和训练趋势等投影必须可由权威记录重建。
- 跨模块只通过明确的 service/interface 或版本化领域事件协作，不直接写其他模块的权威表。
- 业务写入、审计与 outbox 必须在同一事务中完成；Worker 使用租约、幂等键、有限重试和 dead/replay。
- 敏感读取实时复核关系及资源授权，不以成员投影、客户端状态或缓存作为授权源。
- 迁移采用 expand → deploy → contract；历史事实、规则版本和流水不得原地改写。

详细设计见 `docs/architecture.md`、`docs/data-model.md`、`docs/deployment.md` 和 `.trellis/spec/`。

## 3. 开发规则要点

### 3.1 先澄清，再实现

- 先说明会影响行为、架构、数据或范围的假设；低风险细节可合理假定。
- 能从代码、测试、配置或文档确认的事实先自行检查，不向用户重复提问。
- 会显著改变产品行为、数据模型、安全边界或冻结范围的歧义必须先询问。

### 3.2 最小充分方案

- 只实现当前需求；优先复用现有模块、标准库、平台能力和已安装依赖。
- 不为假想扩展增加依赖、wrapper、配置项或抽象层。
- 抽象只用于消除真实重复、明确领域边界或满足明确的架构要求。
- 简洁不能牺牲输入校验、授权、并发、错误处理、数据完整性或安全。

### 3.3 聚焦变更

- 保留现有风格，只修改请求范围及保持一致性所必需的代码。
- 不顺手重构、格式化或删除无关代码；发现无关问题只记录，不擅自修复。
- 删除由本次变更产生的废弃导入、变量、函数和文件。
- 工作区可能包含另一方正在进行的修改；不得覆盖、回退、暂存或提交不属于自己的变更。

### 3.4 可验证结果

- 行为变化优先添加聚焦回归测试；数据库不变量使用真实 PostgreSQL 约束/并发测试。
- 共享数据库测试必须串行运行，避免 TRUNCATE 和并发 runner 相互干扰。
- 日常代码审核默认只检查固定 diff、关键实现与新增/受影响核心代码，并运行对应的聚焦测试；不得每次审核机械重复全量 test、全量 E2E、build 等高成本验证。
- 仅在里程碑签署、合并前、发布前、共享基础设施或跨模块高风险变更、聚焦检查无法提供充分证据时，才按风险扩大到 migration、typecheck、lint、format、build、全量 test 或 E2E；扩大范围须说明原因。
- 未执行项不得表述为通过；可复用 Cursor 在同一固定 SHA 上已有的可信命令证据，Codex 只补充独立的核心聚焦验证。
- Route、错误路径、权限矩阵、DTO、不变量和并发语义必须有可定位证据；“测试全绿”不能替代验收矩阵。

### 3.5 安全操作

- 非快进归并、冲突处理、rebase/reset、历史改写、force-push、部署、数据破坏、权限或密钥操作必须先询问。
- 不在日志、审计或错误中写密码、邀请码明文、训练答案、私密正文、下载令牌或私有链接。

## 4. Codex–Cursor 协作协议

本项目采用“Codex 规划与审核、Cursor 实现”的分工；仓库任务文档和提交 SHA 是事实来源，聊天只用于启动和回报。

- **Codex**：冻结规格、维护任务与阶段指令、审核固定 SHA 并给出 GO/NO-GO；负责文档账务、归并、推送和归档等机械操作，不代写 Cursor 负责的业务代码。
- **Cursor**：只实现当前唯一指令授权的代码、migration/schema 和测试；不得改写规格、任务状态或签署，不得扩大范围、启动下一阶段或自行放行。
- **共享目录**：双方打开同一仓库目录，可直接看到对方变更；正常交接无需 `pull`、`fetch`、切换分支或创建 worktree。开始前只核对分支、HEAD、基线和工作区，不一致即停止，不处理对方未提交变更。
- **阶段提交**：每个 P 阶段只做一次聚焦业务提交，包含实现、测试和实施记录；整改如需提交，也只做一次聚焦整改提交。必要的规划、指令和签署可由 Codex 在阶段边界提交。
- **单阶段交接**：每个 Cursor Prompt 只指向一份唯一仓库指令，并写明 Active task、分支和完整基线 SHA；范围、R/AC-ID、验证及回报格式以该指令为准。
- **审核放行**：Cursor 只能声明“已交审核”。Codex 按固定 SHA 审核；NO-GO 时一次性文档化全部阻断项，GO 后才允许下一阶段。单阶段默认最多经历实现审核、集中整改、最终复验三轮，验收线不得移动。
- **高风险操作**：非快进归并、冲突处理、历史改写、force-push、部署、数据破坏及权限/密钥操作必须先询问。已签署里程碑默认由 Codex 仅快进归并并普通推送。

### 4.1 Claude Code 调用

- 默认用非交互模式执行固定、可复现的检查；仅在需要人工确认、范围选择或提交时使用交互模式。
- 每次调用只做一个明确目标，写明允许的命令、验证范围和超时；不得自行扩展为业务实现或环境修复。
- 全量 test/E2E 每项最多运行一次；超时或无最终结果即记录 blocker，不重复盲跑。E2E 先做短时健康检查，失败则不启动完整用例。
- 回报仅包含提交 SHA、变更范围、命令结果、blocker 和下一条最小建议；证据与实施记录只在本轮结束时更新一次。

## 5. Trellis Workflow

重大功能、新任务或恢复中的实现任务先使用用户级 `trellis-start`，按 `.trellis/` 工作流规划后再实现。简单问答或琐碎文档修改可按比例处理，无需创建 Trellis 任务。

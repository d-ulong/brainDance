import ParentPlansPage from "@/app/parent/plans/page";

/** The reusable library is managed in the context of a student; binding is chosen in its dialog. */
export default function StudentPlansPage() {
  return <ParentPlansPage />;
}

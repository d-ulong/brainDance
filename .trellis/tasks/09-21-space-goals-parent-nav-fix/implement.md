# Implementation plan

1. 用本地数据库列清单确认目标查询需要的列缺失，并记录迁移状态。
2. 修正主题语义色和学生多选组件，增加样式级可读性断言。
3. 移除家长桌面侧栏 CSS，增加桌面导航形态断言。
4. 强化生产启动脚本的迁移、构建、失败中止顺序，并添加静态脚本检查。
5. 对隔离测试数据库应用迁移，运行目标 route 测试、UI 聚焦 E2E、类型、lint、格式和 build。
6. 更新任务报告与经验记录，提交并普通推送 `main`。

# 唯一集中整改指令

Active task: `.trellis/tasks/09-19-dual-theme-ui-refactor`
分支 main；整改基线 `171aa54a7fd9482fadad42374aa6023c00b17621`。

用户要求复核后的结论为 NO-GO。读取本任务 `review-171aa54.md`，一次解决 B01–B08；原 prd/design/implement.md 继续作为冻结验收，禁止删减或改签署。

先核对基线和工作区；保留 Codex 文档、HTML/审查资产、lessons/memo、用户启动脚本与未提交实施报告，不覆盖、不搭带提交。禁止 reset/rebase/force-push、业务服务/API/schema/训练算法改动、部署和试点 DB 操作。

执行顺序：计划 DTO 完整往返与草稿/导航门禁 → 清理导航 CSS 冲突 → 首页区域错误隔离 → 原范围内其余 UI/可访问性补齐 → 最终截图与聚焦回归。内部检查点不形成新阶段或额外等待轮次。若必须改后端合约，先形成精确缺口说明，继续其他独立工作。

必须包含回归：家长原样保存与只改标题不改变计分/时限/时长；改字段后取消/返回/头像/浏览器返回的草稿保护；训练中头像离开取消保持当前会话、确认复用原终止；单一训练摘要失败时日程与积分仍正确显示；手机导航只占自身高度且主操作可点击；周/月选日与凌晨任务可访问；modal 键盘闭环。

运行 `review-check.cjs` 确认字段往返不再失败，并将对应断言纳入项目适当测试（必要时将纯转换逻辑提取为可测模块）。不调用默认破坏性 DB setup。按 implement.md 运行针对性 typecheck/lint/格式及受影响测试，补每个 AC 的证据。

截图必须从新输出目录生成，不复制旧审查图；记录最终 SHA、主题、角色、路由、视口及 mock/真实区别。实际查看完整画面和关键操作，不以脚本启动或有文件即通过。处理 B08 全部缺口。

形成一次聚焦整改提交，更新实施记录，报告完整 SHA、B01–B08 和 AC01–AC10 对照、命令退出码/截图/未执行项，只声明“已交最终复验”，不自行 GO，不推送部署。若存在环境阻塞，诚实报告，不伪造测试或用历史截图替代。

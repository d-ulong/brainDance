# 唯一执行指令：完成原生命周期验收

Active task：`.trellis/tasks/09-19-dual-theme-ui-refactor`
分支：`main`
完整基线：`f98a45a65c5d80c4b4a647c5612926eed34a3869`
日期：2026-09-20。用户已授权“有问题给出 prompt”。本轮只补齐上一份指令中的剩余失败路径，保留已通过的视觉和功能，不新增阶段或需求。

先读根 AGENTS.md、本任务 prd.md、design.md、cursor-state-repair.md 的冻结断言以及 review-f98a45a.md。本文件是本轮唯一执行入口。核对 HEAD/分支/工作区，不匹配则停止并报告；不得覆盖或暂存用户脚本、既有截图、Codex 审核和设计文档等他人修改。

## 修复前完成一张完整状态表

现有修复只覆盖 definition saving，漏了 activation/binding，history clean 用 back 也留下了 forward 占位。必须围绕同一页的所有状态修复，而不是再加局部条件。记录 clean/dirty、保存中、启用中、绑定中、重读失败、离开确认及卸载的转移、可编辑字段和允许操作。保持后端 DTO/service 为事实源，定义/绑定/启用仍是独立操作，不自动连写或修改后端合约。

## S01：启用期间草稿与并发

- 启用正在进行时冻结相关表单输入和保存操作，操作函数也防止并行提交；复用现有字段禁用机制即可，不引入新库。
- 未保存定义仍须先保存再启用。启用失败保留表单并恢复可操作，成功才按原流程离开。若允许离开，须明确不会误报未完成请求，回包不能导航已离开的页面。
- 激活日期按已保存定义/明确操作日期及 Asia/Shanghai 规则取得，不使用页面初次打开时的陈旧日期。不要改后端业务规则。
- 断言：延迟激活 900ms，尝试输入和保存不可操作、无并行 PATCH；失败后草稿可继续编辑；成功离开不丢任何可输入但未提交内容。

## S02：绑定期间目标与其他写操作

- 采用上一轮推荐的最小锁定方案：绑定提交中锁定绑定目标及会与刷新发生竞争的写操作；回包不覆盖请求后的用户意图。已有未保存定义草稿必须完整保留。
- 断言：选择 B 并提交，延迟期间取消 B/勾 C 被明确禁用；不能有“勾选成功后又恢复”的假交互。完成/失败后解锁，现有定义草稿不变。
- 同时覆盖“本次新绑定 A 成功、B 失败”而非 A 一开始就已存在；保留 B 待重试，重试仅发送 B。

## S03：绑定成功、刷新失败的恢复

- 明确区分写入结果和读取状态，保留已知成功及待确认事实，文案准确。增加实际可点击的“重新读取/重试刷新”入口，不依赖 bindingDirty，不再次绑定/解绑已经成功的对象。
- 重读失败保持入口，成功后以返回事实更新；不整页 reload、不丢未保存定义或尚待处理目标。服务端响应中有效日期等字段可用时必须使用，不以 UTC 今日伪造 authority。
- 断言：本次绑定成功→首次 GET 500→关闭错误后仍有恢复入口→点击只发 GET→恢复成功且无重复 POST/DELETE；定义草稿全程一致。

## S04：完整浏览器历史生命周期

- 不再把“clean 时 back 一步”等同于删除占位。处理前进栈、导航方向、目标位置、保存后的占位、卸载/重新进入及直接打开。不要用固定次数 back/go 猜目标，不覆盖 Next 私有路由实现。
- 按公开框架/浏览器能力实现有明确边界的守卫；如现有占位方案无法保持完整语义，先替换状态设计再实现，不叠加更多 suppress 标志。
- 必须验证真实页面操作：列表→编辑→修改→保存→浏览器前进→再修改→后退确认，确认一次后确实回列表。取消则保持当前草稿；后续再尝试仍能正确确认。
- 覆盖至少三轮保存/修改、后退和前进的取消/确认、站内取消/头像离开、直接打开编辑页、重新进入、刷新/关闭。历史长度不能随保存轮数无界增长；一次离开只能确认一次。区分 confirm 与 beforeunload，不能用 confirms>=1 放过两次弹窗。

## 必须执行的验证

- 将 review-f98a45a-browser.cjs 中四条真实失败观察转为确定回归断言，可适配合法的新页面结构，但不得删断言或只检查 URL 包含片段。
- 修正 state-repair-browser.cjs 的弱断言：R01 必须断言无启用请求；R03 必须断言准确目标及确认次数；R04 覆盖本次部分成功和刷新失败恢复。全部 /api/** 拦截 mock，杜绝真实写入。
- 原 review-check.cjs、review-final-check.cjs 和 state-repair-browser.cjs 回归通过；变更源文件 lint/typecheck。运行实际新增失败路径，不能再标“未执行”同时声称该问题修复完成。
- 禁止默认破坏性数据库 setup、试点 DB 操作、迁移或 dev 运行中同目录 build。不要全量回归无关业务。

## 交付边界

一次聚焦提交包含实现、回归测试和实施状态表/证据记录。提交后输出实际完整 SHA、S01–S04 各项断言结果和命令退出码；证据必须绑定最终 SHA，不填写中间提交。截图/结果使用新的临时输出，不覆盖旧证据。不改签署、任务状态或冻结规格；不改训练算法/schema/API，不推送部署，仅声明“已交审核”。Codex 复核通过后按用户授权提交必要记录并普通推送。

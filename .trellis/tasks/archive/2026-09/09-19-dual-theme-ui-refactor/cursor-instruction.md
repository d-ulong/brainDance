# Cursor 唯一实施指令：双主题 UI 重构

Active task: `.trellis/tasks/09-19-dual-theme-ui-refactor`
分支：`main`
完整基线：`f87d495a2136b0729aebd14f67ed1317e78f6f1b`

用户已授权：直接使用 HTML 高保真稿，由 Cursor 完成页面重构，不再生成 Figma，不等待 Figma MCP。Codex 审核最终实现。按本指令完成一轮完整交付，不停在分析或只换首页颜色。

## 必须读取

1. 根目录 AGENTS.md、CONTEXT.md，`.trellis/spec/frontend/` 的组件、hooks、状态、类型、质量规范。
2. 本任务 `prd.md`、`design.md`、`implement.md`：范围、R/AC、不变量、事务/资源边界、并发和失败断言以它们为准。
3. `docs/ui-design/2026-09-19/index.html`、`screen.html`、`theme.css`、`components.html` 与三张 board PNG。实际用浏览器打开观察两主题桌面/手机。HTML 是视觉样板，不是可直接替换业务页的应用代码。
4. `docs/ui-audit/2026-09-18/review.md`、关键截图。历史“未授权实施/不交 Cursor”状态已被本任务新授权覆盖；U10 中“统一默认风格”不覆盖本任务完整双主题要求。

## 执行约束

- 开始先核对分支、HEAD、工作区。若基线/分支不符，停止并报告，不切分支/rebase/reset。允许并保留交接时已知未提交内容：`lessons.md`、`memo.md`、`scripts/start-web-and-worker.bat`、`docs/ui-audit/`、`docs/ui-design/` 和本任务文档；它们不是可覆盖的脏文件。新增不明修改先报告。
- 先在实施记录写路由/入口映射和拟改文件，再连续实现，不再让用户逐项拍板既定低风险样式。未逐页绘制的界面按本任务布局规则和已批准双主题组件延展；遇到必须改 API/授权/业务语义的缺口才停止该依赖并报告，继续独立 UI 工作。
- 覆盖学生首页、家长首页及学生管理、双方计划/日程/计划编辑、训练准备和进行态；共享主题覆盖登录、个人页及其他沿用外壳页面。不要漏掉家长自身资源与指定学生资源区别。
- 复用真实读取、提交、校验、错误与权限逻辑，示例数据不得硬编码。主任务、积分等以实际数据为准；功能保持可操作，禁止 mock API/截图/iframe 代替正式页面。
- 禁止改 `src/db/`、migration、业务服务/API 合约、权限、积分结算、训练时序/算法/计分；禁止添加 UI 框架、外部字体/图标 CDN、部署或修改密钥；禁止改启动脚本。
- 独立编辑页只改变呈现和路由，不把保存/绑定/生成合并。训练外壳重构保留现有 lifecycle hooks、事件队列、失焦与离开/终止协议。
- 所有状态、焦点、异步上下文与防重复提交按 design.md 并发矩阵实现；tokens 必须覆盖 portal、默认/hover/focus/pressed/disabled。
- 使用既有依赖；组件抽取限真实复用，不做无关重构；只格式化变更文件。

## 完成交付

执行 implement.md 聚焦验证，截图双主题两角色桌面/手机并自查与 HTML 的视觉差异；不得仅以编译通过判断成功。实际执行失败需修正，环境阻塞明确列明。

仅提交本次实现、测试、本任务 `implementation-report.md` 与直接相关验收证据，形成一次聚焦业务提交；不得 `git add .` 搭带既有修改，任务规格/状态/签署不由 Cursor 更改。若提交需包含其他方文件，先停在已完成且可审核的工作区报告原因。

最终回报实现 SHA、文件列表、R/AC 对照证据、命令退出码、截图路径、未执行项与风险，并写“已交审核”。不推送、不部署、不自行标记 GO 或启动下一任务。

# 用户重新授权的集中修复指令

日期：2026-09-20。用户在最终 NO-GO 后明确要求 Cursor 继续执行；本指令记录该次授权，不撤销审核结论，不新增产品范围或 P 阶段。

Active task：`.trellis/tasks/09-19-dual-theme-ui-refactor`
分支：`main`
完整基线：`ff1fff84d10084c60c8181229a6e7ec28382015f`

这是本轮唯一执行指令。先读取根 AGENTS.md、本任务 prd.md、design.md、review-ff1fff8-final.md；此前实施指令仅作历史，不叠加额外阶段。开始前核对分支、HEAD 和工作区，若基线不符则停止并报告。保留所有他人未提交文件，尤其启动脚本、lessons.md、memo.md、Codex 任务文档和设计资产，不覆盖、不搭带提交。

## 范围和实现约束

一次处理 F01–F06，维持原 AC01–AC10，不重新设计验收线。

1. **F01 保存与绑定**：恢复定义保存、绑定新增、绑定移除各自明确的操作和成功/失败状态，不由定义保存自动串联绑定或生成。定义成功后同步最新 revision；绑定以服务端返回/重新读取的事实为准。每项失败保留可恢复输入，清楚说明哪项成功、哪项失败；不能将已提交事实描述为整体未保存。绑定操作不能丢失尚未保存的定义草稿。不得用新增后端事务/API 来扩大范围。
2. **F02 Modal**：焦点初始化与真实打开/关闭生命周期绑定，回调更新不得重置焦点；保持最新 onClose。仅顶层弹窗处理 Escape/Tab，关闭顶层后焦点返回合理的底层控件，最终关闭返回触发按钮。新增或编辑草稿的遮罩、关闭按钮、Escape 都必须遵守同一离开保护，不能静默 resetForm。清理监听，避免重复注册和焦点陷阱泄漏。
3. **F03 条目标识**：已有 key 不变；新增条目采用避开全部现有 key 的生成方式，删除、重排、重复新增均保持唯一。React 身份与提交身份一致，不通过重编号改写已有条目身份。
4. **F04 星期选择**：weeklyWeekdays 为周选择唯一输入源，不能从旧 repeatValue 恢复。全不选应给出明确校验并阻止提交；重新选择只提交当前值，保持既有服务端每周至少一天的规则。
5. **F05 草稿保护**：覆盖两角色编辑/新增中的站内链接、取消、头像、浏览器同文档后退/前进、刷新和关闭。取消离开保留路由、字段与选择状态，确认才离开；避免历史记录无限增长、重复确认、返回循环。使用框架允许的方案，不覆盖 Next.js 私有路由内部实现。保存成功才解除保护，失败保留草稿。
6. **F06 冻结设计落地**：按 docs/ui-design/2026-09-19 的 HTML/SVG 资产恢复手机首页的糖果/太空主题表现，尺寸和布局适配 360/390px，不能用简单占位形状代替定稿插画。复杂新增计划迁入独立页面，复用完整编辑字段、校验与离开保护，替换旧新增入口，不残留两套行为。主题装饰不能进入训练刺激区。

Authority 仍是既有后端 service、DTO、授权和训练规则，HTML 仅决定视觉与信息层级。维持既有事务和幂等边界；不得改 schema/migration、计分、训练时序/颜色/随机化或权限。异步读取采用取消或忽略旧响应，卸载清理事件/资源；操作提交中禁用重复触发，失败不清空草稿。若必须变更后端合约才能完成，报告具体缺口，不擅自改动，并继续不依赖该缺口的工作。

## 必须验证的确定断言

| ID | 操作/失败与并发场景 | 断言 |
|---|---|---|
| F01 | 定义保存成功后绑定失败，再重试对应操作；连续保存两次；双击保存 | 定义保持已提交状态且 revision 最新，绑定失败可单独重试；无自动连写或重复并行提交 |
| F01 | 未保存定义时执行绑定；快速切换对象导致旧响应晚到 | 不覆盖草稿，显示与操作对象一致，旧响应不能写入当前页面 |
| F02 | 连续输入中文/英文、父组件重渲染；表单上叠加错误弹窗后按 Escape | 输入焦点不跳走；只关闭顶层，底层草稿保留，Tab 不逃离顶层 |
| F03 | item-1/item-2 删除前者再新增；连续新增/删除/重排 | 所有 key 唯一，已有 key 不变，保存通过原定义校验 |
| F04 | 原周一/周三全部取消，再选周五 | 空选择禁止提交并提示；后续只提交周五 |
| F05 | 从列表进入后修改，浏览器后退取消/确认、前进、刷新；保存失败后离开 | 取消保留完整草稿，确认正常离开，无历史循环；失败不解除保护 |
| F06 | 两主题手机首页及两角色新增/编辑；360/390px、键盘与缩放 | 定稿主题可见、主操作不遮挡、无横向溢出，完整字段可达 |

将 F01–F05 的关键失败路径纳入聚焦回归，尤其真实浏览器输入焦点和历史导航，不能只用截图或静态断言代替。运行 review-check.cjs 与 review-final-check.cjs（不能修改断言掩盖缺陷）；新增项目测试可替代脚本加载方式，但须保留原确定断言。复核原训练导航保护和字段往返没有回归。

仅执行相关测试、受影响 lint/typecheck/格式检查。不要调用带默认数据库 reset/TRUNCATE 的全局 setup；如测试涉及数据库，必须先核对隔离测试库并显式指定，串行运行。试点 dev 服务运行期间禁止在同目录 build。不得迁移或操作试点数据库。

生成新的两主题、两角色、手机和桌面证据；逐项实际查看截图与操作。先完成一次聚焦实现提交，再以该完整 SHA 对应代码验证并在仓库外临时目录生成证据；交付记录 SHA、路由、主题、角色、视口、mock/真实、文件位置和命令退出码，不能复制旧截图或仅记录图片哈希冒充实现 SHA。确实无法验证的项目明确列出，不声称通过。

## 交付

只做一次聚焦修复提交，包含实现、回归测试及实施记录；不暂存本指令或其他 Codex 文档，不改任务状态/审核签署。回报完整 SHA、F01–F06 逐项实现及证据、AC 回归情况、命令退出码和未执行项。最终只声明“已交审核”，不自行 GO、不推送、不部署、不自动启动下一轮。不执行 reset/rebase/force-push 或处理他人未提交变更。

# 执行与证据

1. 固化需求 v1.0、更新领域/产品说明；保留当前 main 的全部既有未提交工作。
2. 修复本机媒体目录/不扫描策略及中文错误，增加策略边界和真实图片验证。
3. 实现计划内容库、版本/绑定/切换、四种重复和多内容日程；集中补约束/迁移及测试。
4. 接入开始/完成/未完成事实与条件积分及更正防重，更新学生执行页面。
5. 独立推送内容/接收管理、tag/日期/学生筛选及媒体/作答授权；增量迁移原记录。
6. 核心聚焦测试、一次类型检查、受影响文件 lint，桌面/手机完整关键路径。任何未完成项明确记录，不能只以纯函数测试宣告全功能可用。
7. 追加 v1.7：迁移多次作答、评论引用与日程覆盖投影；扩展计划服务为并行绑定及优先级重算；将计划/推送/关联迁入学生二级页签并保留旧 URL 跳转；补充多次作答、引用、冲突覆盖与详情聚合的 HTTP/浏览器回归。

当前证据（持续更新）：0034–0038 已由隔离库测试迁移；`plan-definition` 40 项纯逻辑通过，计划库创建/重放/切换/手动生成、开始事实、条件完成 +10 与次日 18:00 未完成 -2 均由隔离库测试覆盖；typecheck 通过。计划库界面已支持多内容、五档积分、编辑和复制。独立推送草稿/发布/筛选授权集成测试通过，页面会对每位接收学生分别上传图片并传入逐学生媒体 ID，本机媒体目录启动检查通过。2026-09-11 已对精确试点库完成备份并在媒体迁移兼容 gate 通过后应用 0034–0038；`plan_library` 表检查由 false 变为 true。计划绑定学生改为独立读取 `/api/family/students` 的有效关联，计划/推送页的失败提示改为可关闭弹窗。保存按钮现显式 submit；计划/推送改为列表默认、弹窗新增；计划已绑定学生可多选添加与移除，推送接收学生可下拉多选/移除。隔离库计划/推送集成测试与桌面、360px 浏览器流程均通过。

8. 新增推送公开期限字段、DTO 和迁移；在服务端建立“作者/家长/到期公开”可见性投影，并用两学生场景覆盖作答、评论和引用泄露边界。
9. 将兑换聚合为学生二级工作台，左侧记录筛选、右侧项目卡片；新增/编辑 Modal 复用现有 idempotent 写入，补聚焦 HTTP 与移动布局证据。
10. 实现可选日期日/周/月日程、收敛返回/退出/二级导航、说明气泡，以及画布手写作答 PNG 上传或受控灰显；最后跑隔离库回归、typecheck、变更文件 lint 和双视口关键 E2E。
11. 修正兑换项目的跨学生家庭共享读取/申请 authority 与按学生月限次；增加“同一家长关联两名学生”的路由回归。将学生管理页签提升到标题区位置，返回同行、退出置顶，并去掉页签上方可见标题。
12. 修复评论回复/引用展示回归并共享详情组件；重排推送详情信息架构与操作栏。
13. 抽取日/周/月日程工作台，移除学生顶级日程页签，在首页与“我的”增加入口，同时复用到家长只读日程。
14. 扩展计划库角色权限和学生计划工作台：学生只管理自有计划、只绑定/生成本人日程，同时只读查看家长计划；补服务与路由聚焦测试。
15. 投影进行中状态并实现带执行区间的直接完成；新增未来待办软清理命令、路由与角色/边界测试。
16. 为兑换申请增加服务端余额门禁和统一确认；集中覆盖取消、删除、停用等高影响操作的确认体验。
17. 收紧月历密度，统一推送库同页详情、浅色正文、可缩放媒体查看器与分层讨论信息流，并完成聚焦页面回归。
18. v2.5：新增目标定义/实例/命令与目标奖励事实，完成家长多学生创建、学生提案、责任家长审批和终态评定；加入手动惩罚扣分与原操作者全额撤销，全部接入流水、余额、审计、outbox 和并发门禁。
19. 增加日/周/月积分期间投影与日程最高分；扩展家长计划库的本人绑定/生成和个人日程读取；家长个人事实不计分。
20. 合并学生计划与日程工作台、调整顶级与“我的”导航、为今日概览统计提供可访问明细浮层；统一所有家长推送入口为同页详情弹框。
21. 聚焦验证：目标生命周期/权限/幂等、扣分余额锁/撤销、期间聚合、家长自用计划；补目标和计划日程关键浏览器路径，执行 typecheck、受影响文件 lint 与 diff check，不运行全量测试或运行中目录 build。
22. v2.6：统一家长单学生上下文标识；重排计划/推送筛选和推送详情媒体/互动层级；图片上限扩为 5 并增加视频链接播放器。
23. 增加目标核心编辑与终态后追加说明的 revision、append-only schema、服务、Route 和家长界面；聚焦验证家长个人计划完整链路。
24. 只运行受影响核心单元/隔离库测试、typecheck、变更文件 lint 和 diff check；运行中 3002 不执行 build 或全量 E2E。

2026-09-12 增量交付证据：评论引用改为服务端授权投影，跨同一 publication 的公开兄弟投递可写、可读且稳定返回作者/摘要；家长与学生详情复用同一引用组件，回复/编辑/删除同行。推送详情已按正文信息、作答时间线和讨论区重排。学生顶级日程页签已移除，首页与“我的”提供入口，共享日历实现日/周/月范围和前后翻页。学生计划工作台可查看家长计划、创建/编辑/启用自有计划并生成本人日程；服务端拒绝跨学生和编辑家长计划，自有计划积分强制为 0。

验证：`typecheck`、`git diff --check` 通过；lint 为 0 error（6 个既有 warning）；评论引用与日历单测 6/6、学生计划核心集成 1/1、跨投递公开引用核心集成 1/1，均通过显式隔离库串行执行。未对运行中的 3002 执行 build，也未对试点库执行测试、迁移或清理。

v2.4 增量：日程查询现在投影 `in_progress`，学生可直接完成并提交真实开始/结束或耗时，服务端校验并按冻结规则自动结算；家长可清除学生未来未开始日程，学生只能清除本人生成的未来未开始日程，日期范围最多 90 天。兑换申请在事务内先锁定并检查余额，审批、撤销、项目停用和评论删除统一确认。月历压缩为单日格，推送库改为浅色卡片与同页详情，图片支持缩略、放大、滚轮/拖动/双指缩放，作答与评论使用带引用摘要的分层讨论流。迁移 0041 已在试点库通过兼容检查并应用；核心日程、兑换、公开引用聚焦测试通过，最终静态复验见下方证据。

最终聚焦复验：状态/月历/引用/公开规则单元测试 12/12，通过；日程开始投影、直接完成及清理集成测试 2/2，通过；兑换创建与审批余额门禁集成测试 1/1，通过；`pnpm typecheck` 与 `git diff --check` 通过；`http://127.0.0.1:3002/` 返回 200 且包含 Next 静态资源引用。本轮未运行全量测试、全量 E2E 或 build。受影响文件 lint 在实现后曾通过；最终复跑受本机锁文件解析出的 `ajv@6.15.0` 包缺少其声明的 `lib/ajv.js` 阻断，属于检查工具依赖缺件，不表述为最终通过。

2026-09-13 启动恢复：上一轮在受限环境中清理 `node_modules` 失败后继续安装，留下半删除依赖树，导致 `pnpm dev` 报缺少 `caniuse-lite/dist/unpacker/agents`。现已在核对绝对目标和端口占用后完整删除生成目录，使用稳定运行时按冻结锁文件干净安装；`pnpm dev` 实际进入 Ready，首页与 HTML 引用的 CSS 静态资源均返回 200。未修改依赖声明、锁文件或数据库，开发服务保持运行。

2026-09-13 v2.5 实施：完成目标定义/实例/责任家长审批与评定、目标奖励流水、手动惩罚和原操作者全额撤销、期间积分汇总；完成家长自用计划/目标/日程、学生计划日程合并、今日概览明细浮层和推送详情 Modal 入口收口。0042 在隔离库与本地闭测试点库均迁移成功；目标/惩罚核心集成及最高分单测 2/2、typecheck、lint 0 error 通过；四个新页面和两个新 API 的未登录 HTTP 冒烟分别返回 200/401，首页及静态资源正常。本轮未运行全量测试、全量 E2E 或 build。

2026-09-13 v2.6 实施：家长单学生计划、推送、日程、训练、兑换、导出和总结页增加统一姓名上下文；计划与推送改为无冗余标题的紧凑筛选。推送附件提升至正文区，学生线程区分已/未作答并分隔作答和评论；图片上下限统一为 0–5，已发布推送编辑追加内容版本，视频直链/YouTube/哔哩哔哩使用白名单播放器。目标新增 revision 编辑和 append-only 评定后说明，家长及学生待审批提案均有入口；“我的”中的家长目标、计划、日程入口保持可达。

v2.6 证据：0043 分别在 `braindance` 与 `braindance_closed_pilot_20260903` 增量迁移成功；目标与个人计划/媒体聚焦回归 13/13，通过；发布后推送编辑 1/1、追加的学生提案编辑/非责任家长拒绝与说明测试 2/2，通过。最终 `pnpm typecheck`、`git diff --check` 通过；lint 0 error（6 个既有 warning）；运行中的 3002 对计划、推送、目标、学生计划和我的页面均返回 200，引用静态资源无 404。未执行全量测试、全量 E2E 或 build。

测试与 schema 变更需保持串行，不对业务库执行 TRUNCATE；数据库和服务操作由主代理统一进行。未经本轮授权不 push、不删除库、不改写历史迁移。
